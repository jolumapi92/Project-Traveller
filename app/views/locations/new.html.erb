<h1>Crear un nuevo destino</h1>

<%= simple_form_for @location do |f| %>
    <%= f.input :name, label: "Name", placeholder: "Capture a particular location" %>
    <%= f.input :description %>
    <%= f.submit "Create Location" %>
<% end %>